//Classic struct with named field
struct Student { name: String, level: u8, remote: bool }

//Tuple struct with data types only
struct grades (char, char, char, char, f32);

//Unit struct
struct unit; 
